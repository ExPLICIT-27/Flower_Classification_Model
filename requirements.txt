#note : these are the libraries for purely running the UI

# Core libraries
streamlit==1.46.1
torch==2.7.1+cu128
torchvision==0.22.1+cu128
albumentations==2.0.8
albumentations.pytorch==1.0.4  
Pillow==11.3.0
numpy==2.1.3

# For visualization
plotly==6.2.0

# Optional but used
warnings  # built-in, no need in requirements
base64    # built-in, no need in requirements
io        # built-in, no need in requirements

# For model loading
tqdm==4.67.1  # (optional, if used during model training or loading)

# Optional: UI polish dependencies (if used separately)
matplotlib==3.10.3  # only if you plan to use it later
